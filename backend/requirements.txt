# Pinned working set. Verified 2026-08-01: `pytest` -> all backend tests pass in
# a clean venv built from this file. Previously every dependency floated, so a
# clean install resolved whatever PyPI served that minute and a security scan of
# the result was not reproducible.

# Web framework
fastapi==0.141.1
starlette==1.3.1
uvicorn[standard]==0.52.0
python-multipart==0.0.32

# Data validation
pydantic[email]==2.13.4
pydantic-settings==2.15.0
email-validator==2.3.0

# Database
sqlalchemy[asyncio]==2.0.51
asyncpg==0.31.0
aiosqlite==0.22.1

# Auth
python-jose[cryptography]==3.5.0
bcrypt==5.0.0

# HTTP client. Runtime, not just test: app/rag/generation.py calls the Groq API
# with it. The test client uses the same pin.
httpx==0.28.1

# Test
pytest==9.1.1
pytest-asyncio==1.4.0

# Retrieval-augmented generation
# pgvector supplies the SQLAlchemy `Vector` column type and the cosine-distance
# operator. The heavy embedding model is NOT here: sentence-transformers pulls in
# torch, hundreds of megabytes that the API image and the CI test job have no use
# for. It lives in requirements-rag.txt. See app/rag/embeddings.py.
pgvector==0.3.6
