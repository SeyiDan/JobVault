{
  "name": "jobvault-extension",
  "version": "1.0.0",
  "private": true,
  "description": "JobVault Chrome extension (MV3). See SECURITY-AUDIT.md.",
  "scripts": {
    "test": "node --test test/escape.test.js",
    "lint": "eslint ."
  },
  "devDependencies": {
    "eslint": "^10.9.1",
    "eslint-plugin-no-unsanitized": "^4.1.2"
  }
}
